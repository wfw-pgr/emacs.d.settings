program main

  use variablesMod
  use ioUtilityMod
  use xyzPois2DMod
  implicit none

  call load__source_and_boundary
  call BiCGSTABCrt2D_MPI( xvec, source, dx, dy, LI, LJ )
  call save__results
  
end program main
  
